image licheepi4a.img {
  hdimage {
  }

  partition boot {
    partition-type = 0xC
    bootable = "true"
    image = "boot.scr"
  }

  partition rootfs {
    partition-type = 0x83
    image = "rootfs.ext2"
  }
}

